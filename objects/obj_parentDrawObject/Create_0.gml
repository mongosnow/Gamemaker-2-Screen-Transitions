/// @description Initialize enum, which draw event, and empty function

enum DRAW
{
	NORMAL = 0,
	GUI = 1,
}

_drawEvent = DEFAULT_DRAWEVENT

function drawEvent()
{
	//EMPTY
	//Overwrite in children
}